export enum NewsType {
  Draft = '2',
  Published = '1',
}

// TODO @hw：应该要用到？在 material-select.vue 里？
export enum MaterialType {
  Image = 'image',
  News = 'news',
  Video = 'video',
  Voice = 'voice',
}
